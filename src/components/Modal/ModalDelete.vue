<template>
  <div
    class="modal-delete d-flex justify-content-center align-items-center"
    @click.self="handleCancel">
    <div class="card">
      <div class="card-header d-flex justify-content-between align-items-center py-3">
        <h3 class="font-weight-bold mb-0">
          {{$t('btn_delete')}}
        </h3>

        <img @click="handleCancel"
             src="../../assets/images/icon-btn-back.svg"
             alt="icon">
      </div>

      <div class="card-body">
        <p class="font-weight-bold">
          {{$t('delete_content')}}
        </p>

        <div class="btn__group">
          <button
            class="btn btn-light btn-pill font-weight-normal"
            @click.prevent="handleCancel"
            v-text="$t('back')"/>
          <button
            class="btn btn-primary btn-pill font-weight-bold"
            @click.prevent="handleAction"
            v-text="$t('popup_btn_delete')"/>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
export default {
  name: 'ModalDelete',

  methods: {
    handleCancel (event) {
      this.$emit('cancel', event)
    },

    handleAction (event) {
      this.$emit('action', event)
    }
  }
}
</script>

<style lang="scss" scoped>
  .modal-delete {
    position: fixed;
    z-index: 10002;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background: rgba(0,0,0,.6);
    .card {
      max-width: 600px;
      width: 100%;
      margin-bottom: 0;
      &-header {
        border-bottom: 1px solid #DDDDDD;
        h3 {
          color: #333333;
        }
        img {
          cursor: pointer;
        }
      }
      &-body {
        text-align: center;
        padding: 40px;
        p {
          color: #333333;
          margin-bottom: 40px;
        }

        .btn__group {
          .btn {
            max-width: 150px;
            width: 100%;
            margin: 0 10px;
          }
        }
      }
    }
  }
</style>
